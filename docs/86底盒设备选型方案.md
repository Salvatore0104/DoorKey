# 86 底盒门禁物联网设备选型方案

## 场景约束

| 项目 | 约束 |
|------|------|
| 安装位置 | 86 底盒（约 80×80×50mm，标准暗盒） |
| 供电 | 12V DC（门禁系统已有电源） |
| 有线网络 | RJ45 门禁专网（172.30.2.0/24） |
| 无线网络 | WiFi 连接家庭 HA 网络 |
| 功能需求 | 开门、视频、对讲 |
| 网络隔离 | 门禁网与家庭网不可二层桥接（安全要求） |

## 核心矛盾

门禁网与家庭 HA 网必须隔离，但设备需要同时访问两个网络：
- **门禁网侧**：接收门口机 UDP 46754 视频、UDP 46753 音频、发送 TCP 46752 控制命令
- **家庭网侧**：通过 WiFi 连接 HA，暴露 HTTP API 供 HA 调用

→ **必须使用双网卡设备**（有线接门禁网 + WiFi 接家庭网）

---

## 方案对比

### 方案 A：树莓派 Zero 2 W + USB 以太网卡（推荐）

#### BOM 清单

| 部件 | 型号 | 尺寸 | 价格 | 说明 |
|------|------|------|------|------|
| 主板 | Raspberry Pi Zero 2 W | 65×30×5mm | ~80元 | 1GHz 四核 ARM，512MB RAM，板载 WiFi |
| 以太网 | USB 2.0 百兆网卡 (RTL8152) | 30×15×8mm | ~15元 | 接门禁网 RJ45 |
| 供电模块 | 12V→5V 降压模块 (MP1584/LM2596) | 45×20×10mm | ~8元 | 门禁12V转5V供树莓派 |
| TF 卡 | 32GB MicroSD | — | ~20元 | 系统盘 |
| USB 扩展 | 微型 USB 2.0 Hub (可选) | 25×20×8mm | ~15元 | Zero 仅1个USB口，需Hub接网卡+声卡 |
| 声卡 | USB 微型声卡 (CM108) | 20×15×8mm | ~10元 | 对讲音频输入输出 |
| 麦克风 | 驻极体麦/I2S MEMS麦 | 10×10mm | ~5元 | 通话上行 |
| 扬声器 | 28mm/40mm 小喇叭 | 28-40mm | ~5元 | 通话下行 |
| 继电器 | 5V 单路继电器模块 | 30×20×15mm | ~5元 | GPIO 控制开门（备用方案） |
| 排针 | 2.54mm 排针 + 杜邦线 | — | ~3元 | 连接 GPIO |

**总成本：约 170 元**（不含继电器备用方案约 160 元）

**总尺寸估算：约 70×45×25mm**（可放入 86 底盒，但较紧凑）

#### 接线图

```
┌─────────────────────────────────────────────────┐
│                  86 底盒内                       │
│                                                  │
│  门禁12V ──→ [12V→5V降压] ──→ Pi Zero 2W (5V)   │
│                                                  │
│  门禁RJ45 ──→ [USB以太网卡] ──→ Pi USB Hub       │
│                                                  │
│  Pi Zero 2W                                      │
│  ├── USB Hub                                     │
│  │   ├── USB以太网卡 ←── 门禁RJ45 (172.30.2.x)   │
│  │   └── USB声卡 ←── 麦克风 + 扬声器             │
│  ├── GPIO 17 ──→ [继电器] ──→ 门禁开门线(备用)   │
│  └── WiFi ──→ 家庭HA网络 (192.168.x.x)          │
│                                                  │
│  门禁开门线 ──→ 继电器常开触点 (并联实体开门键)  │
└─────────────────────────────────────────────────┘
```

#### 网络配置

```bash
# /etc/dhcpcd.conf

# 门禁网卡 (usb0) - 静态IP，无网关，不转发
interface usb0
static ip_address=172.30.2.47/24
nogateway
nohook wpa_supplicant

# WiFi (wlan0) - DHCP 自动获取家庭网IP
interface wlan0
static routers=192.168.1.1
static domain_name_servers=192.168.1.1
```

```bash
# /etc/sysctl.conf — 禁用IP转发，确保网络隔离
net.ipv4.ip_forward=0

# 防火墙：门禁网卡不暴露任何服务
# iptables 规则
iptables -A INPUT -i usb0 -j DROP
iptables -A OUTPUT -o usb0 -m state --state ESTABLISHED,RELATED -j ACCEPT
iptables -A OUTPUT -o usb0 -p tcp --dport 46752 -j ACCEPT  # 仅允许主动连门口机
iptables -A OUTPUT -o usb0 -p udp --dport 46753 -j ACCEPT  # 上行音频
iptables -A OUTPUT -o usb0 -p udp --dport 46754 -j ACCEPT  # 视频请求(如需)
```

#### 软件部署

```bash
# 1. 系统准备
sudo apt update && sudo apt install -y python3-pip python3-venv git

# 2. 克隆项目
cd /opt
git clone https://github.com/Salvatore0104/DoorKey.git hr6107
cd hr6107

# 3. 虚拟环境
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

# 4. 配置环境变量
export HR6107_DEVICE_IP=172.30.2.47
export HR6107_DOOR_IP=172.30.2.36
export HR6107_WEB_HOST=0.0.0.0  # 允许HA网访问
export HR6107_WEB_PORT=8088
export HR6107_AUTH_REQUIRED=1

# 5. systemd 服务
cat > /etc/systemd/system/hr6107.service << 'EOF'
[Unit]
Description=HR-6107 Door Intercom Proxy
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
WorkingDirectory=/opt/hr6107
Environment=HR6107_DEVICE_IP=172.30.2.47
Environment=HR6107_DOOR_IP=172.30.2.36
Environment=HR6107_WEB_HOST=0.0.0.0
Environment=HR6107_WEB_PORT=8088
Environment=HR6107_AUTH_REQUIRED=1
ExecStart=/opt/hr6107/.venv/bin/python run_hr6107.py
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
EOF

sudo systemctl enable hr6107
sudo systemctl start hr6107
```

#### 功能实现

| 功能 | 实现方式 |
|------|----------|
| **视频** | 门禁网卡接收 UDP 46754 H.264，通过 WebRTC 转发给 HA/浏览器 |
| **对讲(下行)** | 门禁网卡接收 UDP 46753 G.711，解码后通过 WebRTC 播放 |
| **对讲(上行)** | 浏览器/HA 麦克风 → WebRTC → 重采样8kHz PCMU → UDP 46753 发门口机 |
| **开门** | TCP 46752 发送 `0003fe` 命令（通话中有效） |
| **来电通知** | TCP 46752 监听呼叫 → WebSocket 推送 → HA 传感器 |
| **继电器开门(备用)** | GPIO 17 触发继电器，并联门禁开门线（独立于网络协议） |

#### 优势与限制

**优势**：
- 直接复用现有 Python 协议代理代码，零移植成本
- 完整的 WebRTC 视频+双向对讲能力
- Linux 环境灵活，可加装 go2rtc/MediaMTX 做 RTSP 转发
- systemd 管理进程，稳定可靠

**限制**：
- 86 底盒空间紧张，USB Hub+网卡+声卡+降压模块堆叠较密
- 树莓派 Zero 2 W 仅 512MB RAM，H.264 解码+WebRTC 可能偏高负载
- USB 百兆网卡性能有限，但门禁视频仅 352×240@25fps 足够
- 需要焊接降压模块和继电器接线

---

### 方案 B：ESP32-S3 + W5500 + 2.4寸LCD（嵌入式方案）

> 更新：经过深入调研，ESP32-S3 使用乐鑫官方 `esp_h264` 软件解码库，在 320×240 分辨率下可达 **19 fps**，门口机视频为 CIF(352×240)，接近此分辨率，**视频解码可行**。G.711 μ-law 编解码在 ESP32 上有成熟实现（ESP32-SIP-Voice 项目）。此方案可做"两手准备"。

#### BOM 清单

| 部件 | 型号 | 尺寸 | 价格 | 说明 |
|------|------|------|------|------|
| 主板 | ESP32-S3-WROOM-1-N16R8 | 25.5×18×3.1mm | ~25元 | 240MHz双核，16MB Flash，**8MB Octal PSRAM**（H.264解码必需） |
| 以太网 | W5500 模块 (SPI接口) | 30×20×5mm | ~12元 | 接门禁网RJ45，硬件TCP/IP栈 |
| LCD屏 | 2.4寸 SPI TFT (ST7789/ILI9341) | 55×40×3mm | ~25元 | 320×240，显示门口视频 |
| 供电 | 12V→5V 降压 (MP1584) + 5V→3.3V AMS1117 | 45×20×10mm | ~10元 | 两级降压：12V→5V(LCD背光)→3.3V(ESP32) |
| 音频功放 | MAX98357A I2S功放 | 20×18×3mm | ~10元 | 下行音频播放（G.711→PCM→I2S） |
| 麦克风 | INMP441 I2S MEMS麦 | 14×14×2mm | ~12元 | 上行音频采集 |
| 扬声器 | 28mm 8Ω 小喇叭 | 28×5mm | ~5元 | 通话下行 |
| 继电器 | 3.3V 光耦隔离继电器模块 | 25×20×15mm | ~6元 | GPIO控制开门（备用方案） |
| 杜邦线/排针 | 2.54mm | — | ~5元 | 连接各模块 |

**总成本：约 110 元**（比树莓派方案省约60元）
**总尺寸：约 70×55×20mm**（更适合 86 底盒，无 USB Hub 堆叠）

#### 接线图

```
┌──────────────────────────────────────────────────────────┐
│                    86 底盒内                              │
│                                                           │
│  门禁12V ──→ [12V→5V降压] ──→ [5V→3.3V AMS1117]          │
│                    │                    │                 │
│                    └→ LCD背光(5V)        └→ ESP32-S3(3.3V)│
│                                                           │
│  门禁RJ45 ──→ [W5500模块] ──SPI──→ ESP32-S3              │
│                                                           │
│  ESP32-S3 引脚分配:                                       │
│  ├── GPIO10-13 (SPI) ──→ W5500 (门禁网)                  │
│  ├── GPIO11-14 (SPI) ──→ 2.4寸 LCD (视频显示)            │
│  ├── GPIO4-6 (I2S) ──→ MAX98357A (下行音频)              │
│  ├── GPIO1-3 (I2S) ──→ INMP441 (上行麦克风)              │
│  ├── GPIO17 ──→ 继电器 (开门控制)                        │
│  └── WiFi ──→ 家庭HA网络 (192.168.x.x)                   │
│                                                           │
│  门禁开门线 ──→ 继电器常开触点 (并联实体开门键)           │
└──────────────────────────────────────────────────────────┘
```

#### 功能实现（基于 ESP-IDF + esp_h264）

| 功能 | 实现方式 | 可行性 |
|------|----------|--------|
| **开门** | TCP 46752 发 `0003fe` 命令 / GPIO继电器 | ✅ 完全可行 |
| **来电监听** | TCP 46752 服务端监听 `call_start` 帧 | ✅ ESP-IDF TCP server |
| **门禁视频接收** | UDP 46754 → 去除25字节私有头 → esp_h264软解 | ✅ **320×240@19fps，CIF基本可行** |
| **视频显示** | H.264解码 → YUV420 → LCD SPI推送 | ✅ 2.4寸 320×240 TFT |
| **对讲(下行)** | UDP 46753 → 去除20字节头 → G.711μ-law→PCM→I2S | ✅ 参考ESP32-SIP-Voice |
| **对讲(上行)** | INMP441→PCM 8kHz→G.711μ-law→加20字节头→UDP 46753 | ✅ 20字节头已逆向 |
| **HA集成** | ESPHome custom component / MQTT / HTTP API | ✅ 多种方式 |
| **视频转发HA** | LCD本地显示为主；可选MJPEG推流到HA | ⚠️ MJPEG推流帧率有限 |

#### 软件架构（ESP-IDF）

```
ESP32-S3 固件
├── main/
│   ├── main.c              # 主任务调度
│   ├── door_protocol.c     # 海尔私有协议（TCP控制+UDP媒体）
│   │   ├── tcp_server.c    # 监听46752，解析call_start/call_end
│   │   ├── tcp_client.c    # 发送answer/hangup/unlock命令
│   │   ├── udp_audio.c     # 接收46753下行+发送上行（G.711+20字节头）
│   │   └── udp_video.c     # 接收46754，去25字节头，喂给H.264解码器
│   ├── audio_codec.c       # G.711 μ-law 编解码（查表法，~1KB ROM）
│   ├── audio_tx_header.c   # 上行音频20字节头模板+序号（offset=3, 1字节）
│   ├── h264_decode.c       # esp_h264 软件解码 → YUV420帧
│   ├── lcd_display.c       # YUV420→RGB565 → SPI推屏 (ST7789/ILI9341)
│   ├── i2s_output.c        # I2S输出下行音频 (MAX98357A)
│   ├── i2s_input.c         # I2S输入上行麦克风 (INMP441)
│   ├── relay_control.c     # GPIO控制继电器开门
│   ├── network_eth.c       # W5500 SPI以太网初始化（门禁网静态IP）
│   ├── network_wifi.c      # WiFi STA模式（家庭HA网）
│   └── ha_api.c            # HTTP/MQTT上报HA（来电通知、状态）
├── components/
│   ├── esp_h264/           # 乐鑫官方H.264解码组件
│   └── w5500/              # W5500驱动
└── sdkconfig.defaults      # PSRAM=on, CPU=240MHz, Octal SPI
```

#### 网络配置（双网卡隔离）

```c
// 门禁网 (W5500以太网) - 静态IP，无网关
eth_config_t:
  ip:      172.30.2.47
  mask:    255.255.255.0
  gateway: 0.0.0.0          // 无网关，阻止跨网段
  dns:     0.0.0.0

// 家庭网 (WiFi STA) - DHCP
wifi_config_t:
  ssid:    "家庭HA网络"
  ip:      DHCP自动获取
  gateway: 192.168.1.1

// 路由隔离：门禁网卡只允许172.30.2.0/24，不设默认路由
// 所有HA通信走WiFi，所有门禁通信走ETH
```

#### 关键技术验证点

| 验证项 | 状态 | 说明 |
|--------|------|------|
| esp_h264 CIF解码帧率 | ⚠️ 需实测 | 官方数据320×240@19fps，352×240预计15-18fps |
| 8MB PSRAM 足够吗 | ✅ | H.264 CIF解码约需2-3MB帧缓冲，8MB充裕 |
| G.711 μ-law编解码 | ✅ 成熟 | 查表法，ESP32-SIP-Voice项目有参考实现 |
| 20字节上行音频头 | ✅ 已逆向 | header_template_hex + sequence_offset=3 |
| W5500+WiFi双网卡 | ✅ 有例程 | esp32-community-projects/esp32-eth-example |
| 2.4寸LCD SPI显示 | ✅ 成熟 | ST7789/ILI9341 驱动完善 |
| 86底盒空间 | ✅ 可装入 | 70×55×20mm，比树莓派方案更紧凑 |

#### 优势与限制

**优势**：
- **成本最低**：约110元，比树莓派省60元
- **尺寸最紧凑**：无USB Hub堆叠，更适合86底盒
- **本地显示**：2.4寸LCD可直接看到门口画面，不依赖手机
- **低功耗**：ESP32-S3约200mA@3.3V，发热小
- **HA原生集成**：ESPHome组件成熟
- **继电器开门**：独立于网络协议，空闲也可开门

**限制**：
- **开发工作量大**：需用ESP-IDF C语言重写协议栈（约2000-3000行代码）
- **视频帧率偏低**：CIF分辨率约15-18fps，不如树莓派流畅
- **无WebRTC**：无法在HA浏览器中实时视频对讲，需改为MJPEG推流
- **调试复杂**：嵌入式开发链路长，排错需要串口+逻辑分析仪
- **音频双工**：I2S全双工需仔细配置DMA，避免采样率不匹配

#### 开发路线图

```
阶段1 (1周): 协议核心 — TCP控制+来电通知+开门命令
  → 验证：HA能收到来电通知，能远程开门

阶段2 (1周): 音频对讲 — G.711编解码+I2S双工+20字节头
  → 验证：能听见门口机声音，门口机能听见本地声音

阶段3 (2周): 视频解码 — UDP接收+esp_h264解码+LCD显示
  → 验证：LCD上看到门口画面（15fps+）

阶段4 (1周): HA集成 — ESPHome组件+MQTT+网络隔离加固
  → 验证：完整功能联调，安全隔离审计

预计总工期：5周（兼职开发）
```

---

### 方案 C：GL.iNet MT300N-V2（迷你路由器方案）

#### BOM 清单

| 部件 | 型号 | 价格 | 说明 |
|------|------|------|------|
| 主机 | GL.iNet MT300N-V2 | ~120元 | 58×58×25mm，双网口，OpenWrt |
| USB 声卡 | CM108 | ~10元 | 对讲音频 |
| TF 卡 | 16GB | ~15元 | 系统 |
| 降压 | 12V→5V (GL.iNet 原生支持5V) | ~8元 | 可选，部分型号支持12V PoE |

**总成本：约 155 元**

#### 优势
- **双网口原生**：WAN口接门禁网，LAN口备用，WiFi接HA网
- **OpenWrt 系统**：内置防火墙，网络隔离配置简单
- **尺寸 58×58×25mm**：完美放入 86 底盒
- **支持 USB 声卡**：可实现对讲

#### 限制
- MT7628N 算力有限（580MHz），H.264 解码可能吃力
- 需要自行编译 Python + aiortc 到 OpenWrt（或用 Docker）
- 仅 128MB RAM

---

## 推荐方案

### 首选：方案 A（树莓派 Zero 2 W + USB 以太网卡）

**理由**：
1. **完整功能**：视频、双向对讲、开门、来电通知全部支持
2. **零移植成本**：直接复用现有 `hr6107/` Python 代码
3. **可扩展性**：后续可加装 go2rtc 做 RTSP、MQTT 桥接等
4. **社区支持**：树莓派生态完善，问题排查容易

### 备选/两手准备：方案 B（ESP32-S3 + W5500 + 2.4寸LCD）

**理由**：
1. **成本更低**：110元 vs 170元，省约35%
2. **尺寸更紧凑**：无USB堆叠，86底盒内更宽松
3. **本地LCD显示**：不依赖手机即可看门口画面
4. **低功耗**：发热小，无需散热孔
5. **继电器开门**：独立于网络协议，空闲也可开门
6. **技术验证充分**：esp_h264 CIF解码可达15-19fps，G.711有成熟实现

**建议策略（两手准备）**：
- **先用树莓派方案快速上线**，验证功能完整性和网络隔离
- **同步开发ESP32固件**，分阶段验证协议/音频/视频
- ESP32固件成熟后，替换树莓派到其他86底盒（如5楼次卧）

### 不推荐：方案 C（GL.iNet MT300N-V2）

**理由**：MT7628N 算力不足，128MB RAM 偏小，性价比不如方案 A 和 B。

---

## 安装步骤（方案 A）

### 1. 硬件组装

```
步骤1: 焊接降压模块
  门禁12V+ → 降压模块IN+ → 降压模块OUT+ → Pi 5V
  门禁12V- → 降压模块IN- → 降压模块OUT- → Pi GND
  (用万用表确认输出5.0V±0.2V后再接Pi)

步骤2: 连接USB设备
  Pi USB口 → USB Hub → USB以太网卡 + USB声卡
  USB以太网卡 RJ45 → 门禁网线

步骤3: 连接音频
  USB声卡 MIC IN → 驻极体麦克风(注意极性)
  USB声卡 SPK OUT → 小喇叭

步骤4: (可选)继电器接线
  Pi GPIO 17 → 继电器 IN
  继电器 COM → 门禁开门线一端
  继电器 NO  → 门禁开门线另一端
  (与实体开门键并联，实体键仍可用)

步骤5: 装入86底盒
  注意绝缘：所有裸露焊点用热缩管包裹
  固定：用双面胶或尼龙柱固定各模块
  散热：树莓派Zero2W负载时约45°C，底盒内需留通气缝
```

### 2. 系统安装

```bash
# 1. 烧录 Raspberry Pi OS Lite (64-bit) 到 TF卡
# 2. 在 boot 分区创建 ssh 空文件启用SSH
# 3. 配置 WiFi: 创建 wpa_supplicant.conf
# 4. 首次启动后 SSH 登录

# 5. 配置门禁网卡静态IP (见上文网络配置)
# 6. 安装项目 (见上文软件部署)
# 7. 启动服务
sudo systemctl enable hr6107
sudo systemctl start hr6107
```

### 3. HA 配置

```yaml
# Home Assistant configuration.yaml
# 添加自定义集成（见 custom_components/haier_hr6107/README.md）

# Lovelace 嵌入可视对讲
type: iframe
url: http://<树莓派Zero家庭网IP>:8088/
aspect_ratio: 70%
```

### 4. 验证测试

```
□ 门禁网卡能 ping 通门口机 172.30.2.36
□ 家庭WiFi能 ping 通树莓派Zero
□ 外机呼叫 → HA 来电传感器触发
□ 浏览器打开可视对讲页面 → 看到门口视频
□ 接听后双向通话正常
□ 通话中点开门 → 门锁打开
□ 门禁网与家庭网隔离验证（从门禁网无法访问HA）
```

---

## 安全注意事项

1. **断电施工**：接线前必须断开门禁12V电源
2. **电压确认**：接树莓派前用万用表确认降压模块输出为 5.0V±0.2V
3. **极性确认**：门禁12V电源正负极需用万用表确认，反接会烧毁设备
4. **绝缘处理**：底盒内所有裸露焊点用热缩管或绝缘胶带包裹
5. **散热**：树莓派 Zero 2 W 满载约 45°C，底盒盖板建议留通气孔
6. **网络隔离验证**：部署后从门禁网侧扫描树莓派，确认仅门禁协议端口可达
7. **不破坏原有门禁**：继电器并联接入开门线，不剪断原有线路，实体开门键仍可正常使用
