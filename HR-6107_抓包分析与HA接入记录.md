# 海尔 HR-6107 抓包分析与 Home Assistant 接入记录

更新时间：2026-06-28  
测试对象：住宅内海尔 HR-6107 门禁智能终端

## 1. 当前硬件状态

- 终端原有红、黑两根供电线，现场观察供电约为 12V；正式取电前仍须使用万用表确认电压、极性和电源余量。
- 终端通过百兆以太网接入小区门禁专网。
- 本轮测试中可能少连接了两根内部排线，因此外机呼叫房号失败。
- 室内终端仍能主动打开门口监控画面，说明网络视频链路可以工作。
- 下一轮呼叫测试前，应在完全断电状态下恢复两根排线，确认插座方向并锁紧排线座，再重新测试响铃、接听、通话和正常开门。

## 2. 已确认的网络信息

| 项目 | 结果 |
| --- | --- |
| 门禁网段 | `172.30.2.0/24` |
| 9 楼 HR-6107/视频接收终端 IP | `172.30.2.55` |
| 9 楼 HR-6107/视频接收终端 MAC | `00:07:a8:89:9b:cf` |
| 门口机/视频源 IP | `172.30.2.36` |
| 门禁网关 | `172.30.2.254` |
| 管理服务器 | `172.30.100.50`、`172.30.100.100` |
| 海尔设备发现端口 | UDP `7083` |
| 视频目标端口 | UDP `46754` |
| 控制目标端口 | TCP `46752` |
| DHCP | 未发现可用 DHCP，终端使用固定 IP |

电脑在未设置静态地址时自动获得过 `169.254.239.64`。这是 Windows 在 DHCP 失败后生成的链路本地地址，不属于门禁系统地址。

## 3. 海尔设备发现协议

门禁网中存在大量海尔设备周期性发送的数据，主要特征为：

- UDP 源端口和目标端口均为 `7083`。
- 发现报文中可以看到 `Haier` 和 `UDISCOVERY_PAD` 标识。
- 多台设备位于 `172.30.2.0/24`，MAC 地址厂商前缀由 Wireshark 识别为海尔。
- 该协议是海尔私有发现协议，不是 mDNS、ONVIF 或 SIP。

## 4. 主动监控视频测试结果

2026-06-28 进行主动查看门口监控测试，成功捕获三段视频流：

- 视频方向：`172.30.2.36` → `172.30.2.55`
- 传输方式：私有 UDP 分片
- UDP 目标端口：`46754`
- 控制连接：TCP `46752`
- 视频编码：H.264 Baseline
- 分辨率：352 × 240
- 平均帧率：25 FPS
- 已还原视频时长：42.68 秒
- 已还原帧数：1067 帧
- MP4 文件大小：约 4.99 MB

每个视频 UDP 包前有 25 字节海尔私有分片头。去掉该私有头后，可以找到标准 H.264 Annex-B 起始码以及 SPS、PPS、IDR 数据。因此视频能够重新拼接并封装为 MP4。

本轮没有发现标准 RTSP、SIP、RTP 或 ONVIF 会话。当前结论是：

> HR-6107 使用海尔私有控制协议，通过 UDP 发送带私有分片头的裸 H.264 视频，而不是直接提供可供 Home Assistant 使用的 RTSP 地址。

## 5. 已生成的测试文件

| 文件 | 用途 |
| --- | --- |
| `hr6107_boot_call.pcapng` | 初次启动和网络发现抓包 |
| `hr6107_live_test.pcapng` | 主动监控过程的完整抓包 |
| `hr6107_monitor.h264` | 去掉 25 字节私有包头后拼接的原始 H.264 |
| `hr6107_monitor.mp4` | 可直接播放的测试视频 |

视频中存在少量解码损坏，可能来自抓包丢包、交换机转发方式，或三段独立监控流直接拼接。主体画面可以正常还原。

## 6. Home Assistant 接入方向

由于设备不提供标准 RTSP，不能直接把 `172.30.2.36` 或 `172.30.2.55` 填入 HA 的 Generic Camera。建议增加一个长期运行的协议代理：

```text
海尔门口机 172.30.2.36
          │ 私有控制 + UDP/H.264
          ▼
隔离的 OpenWrt/Linux 代理
          │ 去除25字节私有头并封装
          ├── RTSP
          └── 可选：事件/MQTT
          ▼
Home Assistant / 手机
```

代理需要完成：

1. 接收或复制发往 `172.30.2.55:46754` 的 UDP 视频包。
2. 按海尔私有分片序号重组数据，并去除每包 25 字节私有头。
3. 将 H.264 发布为 RTSP，推荐使用 MediaMTX 或 go2rtc 承载输出。
4. HA 使用 Generic Camera、FFmpeg Camera 或 go2rtc 接入代理提供的 RTSP。
5. 后续从一次完整呼叫中识别响铃、接听、挂断等控制事件，再转换成 HA 事件或 MQTT 消息。

现阶段只确认了视频方向，尚未确认如何主动请求视频。被动代理若部署在普通交换机旁路端口，可能看不到单播流量；可能需要以下方式之一：

- 代理设备串联在墙内网线和 HR-6107 之间；
- 使用支持端口镜像的交换机；
- 解析 TCP `46752` 控制协议，让代理以合法终端身份请求自己的门口机视频。

## 7. 网络隔离要求

- 不应把小区门禁网与家庭 LAN 直接做二层桥接。
- 推荐使用双网口 OpenWrt/Linux 设备隔离门禁网，并通过独立 Wi-Fi 管理接口连接家庭 IoT 网络。
- 防火墙仅允许 Home Assistant 访问视频代理，不允许门禁网访问家庭其他设备。
- 不把门禁、摄像头或代理端口直接映射到公网；手机远程访问应通过 Home Assistant 官方远程访问或家庭 VPN。
- 后续测试只针对本人住宅终端及本人发起的呼叫，不采集其他住户的音视频。

## 8. 下一次测试计划

1. 断开 12V 电源。
2. 恢复并锁紧缺少的两根排线，确认没有反插或偏针。
3. 重新通电，确认屏幕、触摸、麦克风、扬声器和按键正常。
4. 开始抓包后，让外机呼叫本房号。
5. 响铃约 5 秒后接听，双向通话约 10 秒。
6. 在室内终端上按一次正常开门键，等待约 5 秒后挂断。
7. 分别标记响铃、接听、通话、开门和挂断时间，以便区分各类控制报文。
8. 对比主动监控和外机呼叫产生的 TCP/UDP 会话，确定呼叫信令以及音频编码。

## 9. 当前结论

- HR-6107 的固定地址和主要服务器地址已经确认。
- 主动监控视频已经成功抓取、识别并还原。
- 视频为标准 H.264，但外层采用海尔私有 UDP 分片协议。
- 呼叫失败目前不能归因于网络协议；缺少两根排线是更直接的硬件原因，须恢复排线后重新验证。
- HA 接入技术上可行，下一关键步骤是获取一次完整的正常呼叫抓包，并实现私有 UDP/H.264 到 RTSP 的代理。

## 10. 楼上终端正常呼叫测试

2026-06-28 使用楼上可正常工作的门禁终端完成了响铃、接听、通话、正常开门和挂断测试。有效抓包为：

- `hr6107_upstairs_test_retry.pcapng`
- 抓包时长：171.21 秒
- 数据包数量：6984
- 抓包大小：约 3.35 MB

注意：`hr6107_upstairs_test.pcapng` 是一次误选 Intel Wi-Fi 网卡产生的无效抓包，只包含电脑家庭网络流量，不应用于门禁协议分析。

### 10.1 呼叫视频

- 视频源：`172.30.2.36`
- 室内终端：`172.30.2.55`
- 视频端口：UDP `46754`
- 视频开始：抓包相对时间 136.153 秒
- 视频结束：抓包相对时间 156.769 秒
- 持续约 20.62 秒
- 编码仍为 H.264，私有分片头仍为每包 25 字节

这说明正常来电时，终端会在用户接听之前先建立视频流，用于显示来访者画面。

### 10.2 呼叫音频

- 音频源：`172.30.2.36`
- 室内终端：`172.30.2.55`
- 音频端口：UDP `46753`
- 音频开始：抓包相对时间 142.156 秒
- 音频结束：约 156.876 秒
- 持续约 14.72 秒
- 包数量：231
- 每包音频数据：512 字节
- 私有音频头：每包 20 字节
- 当前成功解码方式：G.711 μ-law、8000 Hz、单声道

音频比视频晚约 6.00 秒开始，与“先响铃、后接听通话”的操作顺序一致。最终编码判断应以试听 `hr6107_call_audio.wav` 是否语音自然为准。

### 10.3 控制链路

正常呼叫仍使用 TCP `46752` 传输海尔私有控制报文，关键时间点包括：

- 135.478 秒：门口机向室内终端发送呼叫开始控制报文。
- 136.153 秒：视频开始。
- 142.156 秒：音频开始，可视为接听阶段。
- 156.769 秒附近：视频和音频结束，可视为挂断阶段。
- 162.179 秒：管理服务器 `172.30.100.100` 向终端发送一条后续控制报文，具体含义尚待单独事件测试确认。

由于当前旁路抓包主要看到发往 `172.30.2.55` 的单向数据，室内终端发出的接听、开门等上行控制包可能没有被交换机复制到电脑。因此不能仅凭本次抓包准确指定开门报文。

### 10.4 与主动监控的区别

| 项目 | 主动查看监控 | 正常外机呼叫 |
| --- | --- | --- |
| 视频 | UDP `46754`，H.264 | UDP `46754`，H.264 |
| 音频 | 未捕获 | UDP `46753`，G.711 μ-law 8 kHz 单声道 |
| 视频出现时间 | 用户主动请求后 | 接听前、响铃阶段即出现 |
| 音频出现时间 | 无 | 接听后约 6 秒出现 |
| 控制 | TCP `46752` | TCP `46752`，并有管理服务器后续控制 |

### 10.5 本轮生成文件

| 文件 | 用途 |
| --- | --- |
| `hr6107_call_video.h264` | 去除私有分片头后的呼叫原始视频 |
| `hr6107_call_video.mp4` | 封装后的呼叫视频 |
| `hr6107_call_audio.mulaw` | 去除私有头后的原始 G.711 μ-law 音频 |
| `hr6107_call_audio.wav` | 可直接试听的通话音频 |
| `hr6107_call_with_audio.mp4` | 按约 6.003 秒偏移合并的音视频测试文件 |

下一步若要精确识别响铃、接听、开门和挂断事件，应使用真正的双向端口镜像或串联透明网桥，并分别进行“一次只操作一个动作”的对照测试。

## 11. 单独开门对照测试

有效抓包文件：`hr6107_open_only_test.pcapng`

- 抓包时长：33.49 秒
- 呼叫开始控制报文：3.511 秒，`172.30.2.36 → 172.30.2.55:46752`
- 视频：4.156–21.090 秒，UDP `46754`
- 音频：17.214–21.119 秒，UDP `46753`
- 呼叫结束控制报文：21.091 秒，TCP `46752`
- 在 10.450、10.779、17.005 和 21.022 秒附近观察到室内终端主动访问门口机 TCP `46752` 后产生的反向握手/关闭包。

本次测试进一步证明接听、开门和挂断等上行动作通过室内终端主动访问门口机的 TCP `46752` 完成。但当前普通交换机只把门口机到室内终端的下行数据复制给电脑，真正的上行 TCP 载荷没有被捕获。因此，目前不能可靠判断上述四个时间点中哪一个载荷代表开门，也不能得到可复现的开门指令。

要继续识别事件，需要将 HR-6107 端口的收发双向流量镜像到电脑，或者把透明双网口抓包设备串联在墙内网线与终端之间。

## 12. 无呼叫、仅按实体开门键测试

有效抓包文件：`hr6107_button_only_test.pcapng`

- 抓包时长：63.97 秒
- 数据包数量：496
- 测试动作：无呼叫状态下只按一次实体开门键
- 抓包结果：没有观察到任何涉及 `172.30.2.55` 的新增 TCP、UDP、ARP 或 ICMP 数据，也没有门口机或管理服务器向该终端返回可识别的开门确认。

这个结果只能证明当前旁路抓包没有看到开门后的下行反馈，不能证明开门没有使用网络。可能性包括：

1. 开门键通过排线或本地硬件触点控制；
2. 室内终端通过网络发送单向开门请求，但门口机不返回确认；
3. 门口机的响应发往其他控制器或使用当前普通交换机未复制的方向。

由于当前抓包口看不到 `172.30.2.55` 发出的上行单播，必须获得双向端口镜像后才能在以上可能性之间作出判断。

## 13. 使用 5 楼闲置终端网线进行的网络测试

此前的正常通话、主动监控和按键抓包均在 9 楼进行。此测试位于 5 楼：9 楼 HR-6107 仍然在线，拆除的是 5 楼终端，电脑接入 5 楼终端留下的网线。此前将两个楼层混淆，以下为修正后的结论：

- 抓包电脑有线地址：`172.30.2.200`
- 网关 `172.30.2.254`：Ping 正常，0% 丢包，约 1–10 ms
- 门口机 `172.30.2.36`：Ping 正常，0% 丢包，小于 1 ms
- 9 楼终端 `172.30.2.55`：仍能正常 Ping，MAC 为 `00:07:a8:89:9b:cf`，符合 9 楼终端仍在线的实际状态
- 20 秒空闲抓包未发现 UDP `46753/46754` 或 TCP `46752` 媒体会话
- 抓包文件：`hr6107_terminal_removed_idle.pcapng`

因此，5 楼这根闲置网线与 9 楼终端处于同一门禁网段，并能访问网关、门口机和 9 楼终端。5 楼被拆终端的原 IP 尚未确定。门口机不会在空闲状态持续发送视频，必须由终端发起监视或由外机呼叫后才建立私有视频流。

## 14. 5 楼损坏终端隔离开机测试

将 5 楼损坏终端的网口直接连接电脑 ASIX USB 有线网卡后进行测试：

- Windows 网卡状态：`Disconnected`
- 介质连接状态：`Disconnected`
- 协商速率：`0 bps`
- 10 秒抓包：0 个数据包
- 抓包文件：`hr6107_floor5_broken_boot.pcapng`

这表示故障发生在以太网物理链路层，尚未进入固定 IP 或协议通信阶段。可能原因包括终端未正常供电、主板未启动、以太网 PHY/网络变压器故障、RJ45 焊点损坏或网线故障。固定 IP 设置错误不会导致物理链路灯完全不亮。

## 15. 5 楼门禁网被动设备盘点

在 5 楼墙内网线接入电脑后进行约 20 秒被动监听：

- 链路：100 Mbps
- 电脑地址：`172.30.2.200/24`
- 被动观察到 71 个唯一的海尔 IPv4/MAC 组合
- 这些设备均周期性发送 UDP `7083` 海尔发现报文
- 另观察到网关 `172.30.2.254`
- 抓包文件：`floor5_passive_inventory.pcapng`

该数字表示短时间内主动发出流量的可见海尔设备，不等同于网段内所有配置过或离线的设备总数。

## 16. 外机两次呼叫 501 失败测试

抓包文件：`hr6107_floor5_call501_failed.pcapng`

两次失败呼叫产生了高度一致的 ARP 行为：

- 门口机：`172.30.2.36`，MAC `00:07:a8:89:95:c9`
- 第一次呼叫：在相对时间 26.249、27.249、28.249 秒连续询问 `172.30.2.47`
- 第二次呼叫：在相对时间 44.229、45.228、46.229 秒再次连续询问 `172.30.2.47`
- ARP 请求均未收到回应
- 未建立任何 TCP 会话
- 未发送 UDP `46753/46754` 音视频

由此可以确认：

> 501 原终端固定 IP 为 `172.30.2.47`。

呼叫失败发生在最初的 ARP 地址解析阶段。门口机无法获得 `172.30.2.47` 对应的 MAC 地址，因此没有进入 TCP `46752` 控制、UDP `46754` 视频或 UDP `46753` 音频阶段。当前抓包无法得出 501 原终端 MAC；需要从损坏终端主板、贴纸、存储配置或可恢复的启动日志中获取。

## 17. 电脑使用 501 地址的呼叫测试

将电脑有线接口设置为 `172.30.2.47/24`，不设置网关，不伪造原终端 MAC。抓包文件：`hr6107_pc_as_501_ip_test.pcapng`。

测试结果：

- 门口机 `172.30.2.36` ARP 查询 `172.30.2.47`
- 电脑以自身 MAC `ac:1a:3d:85:1e:16` 成功返回 ARP 应答
- 门口机接受该 MAC，并向 `172.30.2.47:46752` 发起 TCP SYN
- 第一次连接尝试：44.278 秒，47.268 秒重试
- 第二次连接尝试：54.035 秒，57.028 秒重试
- 电脑未运行海尔终端控制服务，没有返回 TCP SYN-ACK，也没有建立会话
- 未出现 UDP `46754` 视频或 UDP `46753` 音频

因此，501 呼叫失败点已从“ARP 找不到终端”推进到“TCP `46752` 控制服务没有响应”。该测试也表明门口机会接受 `172.30.2.47` 当前解析到的新 MAC，至少在建立初始 TCP 连接时不强制要求原终端 MAC。若继续研究呼叫协议，下一步应仅运行一个受控、只记录入站数据的 TCP `46752` 测试监听器，不发送接听或开门控制。

## 18. TCP 46752 只读监听测试

电脑继续使用 `172.30.2.47/24`，在 TCP `46752` 启动仅接收入站数据的测试监听器；防火墙仅允许门口机 `172.30.2.36` 访问。监听器不发送任何海尔应用层数据。

相关文件：

- `hr6107_tcp_listener_call_test.pcapng`
- `haier_46752_listener.log`
- `haier_46752_connection_1.bin` 至 `haier_46752_connection_4.bin`

测试结果：

- 共完成 4 次 TCP 三次握手
- 每次均由门口机 `172.30.2.36` 主动连接 `172.30.2.47:46752`
- 每次握手后，门口机均发送相同的 34 字节呼叫开始通知
- 34 字节数据与 9 楼正常呼叫抓包中的呼叫开始控制报文一致
- 门口机发送通知后主动关闭连接
- 外机仍提示呼叫失败，未建立音视频流

对比 9 楼正常呼叫可见：正常终端收到 34 字节通知后，会在约几十毫秒内主动反向连接门口机 `172.30.2.36:46752`。因此只监听入站端口不足以完成呼叫；还需要终端发起反向 TCP 连接并发送正确的海尔应用层响应。当前旁路抓包没有捕获到 9 楼终端的上行载荷，不能安全、可靠地构造该响应。

## 19. 501 本地网页仪表盘

已建立本地网页控制台：`http://127.0.0.1:8088`

相关文件：

- `haier_dashboard.py`：TCP 来电监听与本地 HTTP API
- `haier_dashboard.html`：网页界面
- `haier_dashboard.log`：网页服务及通信日志

当前功能：

- 监听 `172.30.2.47:46752`
- 防火墙仅允许门口机 `172.30.2.36` 访问监听端口
- 实时显示监听状态、来电状态、来电次数、RX/TX 字节数
- 实时显示系统、接收和发送日志以及入站报文十六进制
- 收到门口机 34 字节来电通知后，页面显示“501 正在呼叫”约 15 秒

开门和接听按钮已保留在界面中，但当前安全禁用。原因是尚未获得并验证真实终端的上行应用层协议，网页不会猜测或发送开门、接听控制数据。

## 20. 501 软件终端 v2（2026-06-28）

旧版只读仪表盘已升级为异步软件终端，主入口为 `run_hr6107.py`，网页仍为
`http://127.0.0.1:8088/`。当前服务实际监听：

- TCP `172.30.2.47:46752`：接收门口机控制连接，按 `ffff + 大端长度` 精确读取完整帧。
- UDP `172.30.2.47:46753`：20 字节私有头后按 G.711 μ-law、8 kHz 单声道解码。
- UDP `172.30.2.47:46754`：去除 25 字节私有头后送入 H.264 解码器。
- HTTP/WebSocket `127.0.0.1:8088`：网页、认证 API、状态和结构化实时日志。

已实现：

- `IDLE → RINGING → CONNECTING → ACTIVE → ENDING → IDLE` 状态机。
- 浏览器通知、提示音、WebRTC 视频/门口音频、浏览器麦克风采集入口。
- 令牌认证、HttpOnly 会话、固定门口机目标、开门 10 秒限频和审计日志。
- `GET /api/state`、`GET /api/logs`、`POST /api/call/answer`、
  `POST /api/call/hangup`、`POST /api/unlock`、`POST /api/webrtc/offer`、
  WebSocket `/ws/events`。
- Home Assistant 自定义组件：来电二元传感器、通话状态、服务状态和开门按钮。
- 901 双向抓包动作标记工具 `tools/capture_901.py`，以及方向验证和动作窗口分析工具
  `tools/analyze_901.py`。
- 浏览器麦克风到 8 kHz PCMU 的重采样、分包、序号字段写入和 UDP 发送器；只有
  协议配置被人工标为已验证后才会运行。

协议安全门位于 `protocol_profile.json`。当前 `verified` 为 `false`，接听、挂断、
开门和麦克风上行不会发送。重新分析 `hr6107_upstairs_test_retry.pcapng` 的结果仍是：

- `door_to_terminal_seen = true`
- `terminal_to_door_seen = false`
- `bidirectional = false`

因此旧抓包不能用于启用控制命令。必须按 `tools/README.md` 使用端口镜像或网络 TAP
完成 901 真正双向采集，并对每项操作的三次样本进行字段核对。
